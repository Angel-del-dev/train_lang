retNum all := ({
    numeric one = 1;
    pl(one);
});

retNum2 all := ({
    numeric two = 2.4;
    pl(two);
});

_ main := ({
    retNum();
    retNum2();
    numeric sum = +(1, 2.4);
    string delimiter = ", ";
    string concat = concat(delimiter, 'test', 'test2');
    
    pl(sum);
    pl(concat);
});